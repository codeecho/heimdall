export default class Device{

    constructor(ipAddress, name, rootDirectory, location){
        this.ipAddress = ipAddress;
        this.name = name;
        this.rootDirectory = rootDirectory;
        this.location = location;
    }

}